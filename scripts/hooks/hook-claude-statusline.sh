#!/usr/bin/env bash
# Optional statusLine: usage is reduced off the agent's foreground path.
bonsai_agent=claude
bonsai_event=StatusLine
# shellcheck source=_dispatch.sh
. "${BASH_SOURCE[0]%/*}/_dispatch.sh"
